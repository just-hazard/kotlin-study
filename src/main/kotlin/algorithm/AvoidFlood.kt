package algorithm

class AvoidFlood {
    // rains [1,2,3,4,5] 배열 인덱스의 번호 (호수 번호) / 인덱스 순서 (비가 오는 날짜 순서 예: 첫째 날, 둘째 날)
    // 호수에 비가 1번 오게되면 결과값 -1
    // 호수 중 하나라도 범람을 하게 되면 output [] 반환
    // 인덱스에 호수 번호 대신 0의 값이 들어온다면 특정한 호수를 말릴 수 있다 answer에는 말린 호수 번호 표기

    fun avoidFlood(rains: IntArray): IntArray {
        return rains
    }
}
